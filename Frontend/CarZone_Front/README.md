Servir o frontend (desenvolvimento)

Recomendo usar o script npm abaixo para servir a pasta `Frontend/CarZone_Front` localmente.

Instalação (apenas uma vez):

```bash
npm install
```

Rodar o servidor localmente (porta 5000):

```bash
npm start
```

Alternativa: usar a extensão Live Server do VSCode.
